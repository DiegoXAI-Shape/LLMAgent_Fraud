pandas>=2.2
openpyxl>=3.1
networkx>=3.2
# Lectura de PDF: extrae la capa de texto y, si el PDF viene escaneado,
# rasteriza la página a imagen para pasarla por visión. Se eligió PyMuPDF
# porque cubre los dos casos con una sola dependencia y sin binarios externos
# (pdf2image exige instalar poppler aparte, que en Windows es un dolor).
pymupdf>=1.24
pillow>=10.0
Faker>=25.0
ollama>=0.4
google-genai>=0.3
python-dotenv>=1.0
