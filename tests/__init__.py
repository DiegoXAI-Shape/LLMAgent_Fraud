"""Suite de pruebas del agente forense.

Existe este archivo porque `python -m unittest discover -s tests -t .` exige que
el directorio sea un paquete importable; sin él la ejecución falla con
"Start directory is not importable".
"""
